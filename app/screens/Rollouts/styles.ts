import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  login: {
    padding: 8,
  },
  forgot: {
    marginRight: 0,
    flex: 1,
    color: '#321fdb',
    textAlign: 'right',
    fontSize: 12,
  },
  buttonStyle: { backgroundColor: '#2819ae', marginLeft: 10 },
  textColor: { color: 'white' },
});

export default styles;
